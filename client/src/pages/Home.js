import React from 'react';

const HOME = () => {
    return (
        <div>
            <h1>HOME</h1>
            {/* <Link to="/login">Login</Link>
            <Link to="/logout">Logout</Link> */}

        </div>
    )
};

export default HOME;